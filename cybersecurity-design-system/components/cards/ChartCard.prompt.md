Framing card for any chart component (LineChart, BarChart, DonutChart, Heatmap…) — gives it a title, subtitle, and optional legend/footer row, consistent across the dashboard grid.

```jsx
<ChartCard title="Blocked attacks" subtitle="Last 7 days" chart={<LineChart data={weeklyData} />} />
```

Inventory card for the API/asset catalog grid — icon, name, type, health dot, and a quick endpoint/scan-recency summary.

```jsx
<AssetCard name="payments-api" type="Internal API" status="warning" endpointCount={18} lastScanned="2h ago" icon={<Icon name="server" size={16}/>} />
```

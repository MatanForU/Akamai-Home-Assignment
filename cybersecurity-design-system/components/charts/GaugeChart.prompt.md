Semicircular gauge for a single score — overall risk score, compliance %, posture score.

```jsx
<GaugeChart value={72} max={100} label="Security posture" color="var(--accent-primary)" />
```

Color the arc by severity when the score represents risk (e.g. red for a low compliance score) or brand teal for a positive-framed metric.

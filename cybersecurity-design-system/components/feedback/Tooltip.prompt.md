Hover label for icon buttons, truncated IDs, and chart data points. Always dark, regardless of theme, for contrast.

```jsx
<Tooltip label="Copy request ID"><IconButton icon={<Icon name="copy" size={14}/>} aria-label="Copy" /></Tooltip>
```

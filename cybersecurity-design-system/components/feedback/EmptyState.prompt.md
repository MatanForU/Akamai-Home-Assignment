Zero-data state for empty tables (no alerts, no assets yet) — reassuring, not alarming, matches the "calm SecOps" tone.

```jsx
<EmptyState icon={<Icon name="shield-check" size={20}/>} title="No open critical alerts" description="You're all caught up. New findings will appear here." />
```

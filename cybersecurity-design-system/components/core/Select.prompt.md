Native-backed dropdown styled to match Input. Used for filters (severity, time range, environment) across tables and dashboards.

```jsx
<Select label="Severity" value="all" options={[{value:"all",label:"All severities"},{value:"critical",label:"Critical"}]} />
```

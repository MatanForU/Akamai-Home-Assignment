Page header bar (60px) — screen title, optional breadcrumb caption above it, and right-aligned search/action slot.

```jsx
<Topbar title="API Inventory" breadcrumb="Security / Assets" actions={<Button size="sm">New scan</Button>} />
```
